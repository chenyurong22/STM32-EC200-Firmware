// Placeholder — app requires Firebase and auth, not suitable for unit widget tests.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
